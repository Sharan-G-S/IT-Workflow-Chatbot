# Application Flow Diagram

## User Journey

```
┌─────────────────────────────────────────────────────────────────┐
│                     Browser Opens App                            │
│                   http://localhost:3000                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
                   index.html
                         │
                         │ Redirects to
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      login.html                                  │
│  ┌────────────────────────────────────────────────────┐         │
│  │  Email: _____________________                      │         │
│  │  Password: __________________                      │         │
│  │  [Sign In Button]                                  │         │
│  │                                                     │         │
│  │  Don't have account? → signup.html                 │         │
│  └────────────────────────────────────────────────────┘         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ POST /api/auth/login
                         │ Session created
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    dashboard.html                                │
│  ┌──────────────────────────────────────────────────┐           │
│  │ 🤖 IT Workflow Assistant    [UserName] [Logout]  │           │
│  └──────────────────────────────────────────────────┘           │
│                                                                  │
│  [AI Chat] [Tickets] [Access Requests] [Onboarding]             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━             │
│                                                                  │
│  ┌─ AI Chat Tab ─────────────────────────────────┐              │
│  │                                                │              │
│  │  Choose: [Request Access ▼]                   │              │
│  │                                                │              │
│  │  💬 Chat Messages:                            │              │
│  │  ┌─────────────────────────────────────────┐  │              │
│  │  │ User: I need access to Figma           │  │              │
│  │  │ Bot: Access request created! ID: 123   │  │              │
│  │  └─────────────────────────────────────────┘  │              │
│  │                                                │              │
│  │  [Type message...] [Send]                     │              │
│  └────────────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Architecture

```
┌──────────────┐
│   Browser    │
│  (Frontend)  │
└──────┬───────┘
       │
       │ HTTP Requests (JSON)
       │
       ▼
┌──────────────────────────────────────────────────────────┐
│                    Express Server                         │
│                     (server.js)                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │  Middleware:                                       │  │
│  │  - body-parser                                     │  │
│  │  - express-session (auth)                          │  │
│  │  - CORS                                            │  │
│  │  - static file serving                             │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  Routes:                                                  │
│  ┌─────────────┬─────────────┬──────────────────────┐   │
│  │ Auth Routes │ Data Routes │  AI Chat Routes      │   │
│  │ /api/auth/* │ /api/tickets│  /api/chat/access    │   │
│  │             │ /api/access-│  /api/chat/tickets   │   │
│  │             │  requests   │  /api/chat/onboarding│   │
│  │             │ /api/onboard│                      │   │
│  └──────┬──────┴──────┬──────┴───────┬──────────────┘   │
│         │             │              │                   │
└─────────┼─────────────┼──────────────┼───────────────────┘
          │             │              │
          ▼             ▼              ▼
┌─────────────┐  ┌──────────────┐  ┌────────────────┐
│ authService │  │ Data Services│  │ AI Integration │
│             │  │ - tickets    │  │ - OpenAI API   │
│ - hash pwd  │  │ - access req │  │ - prompts      │
│ - verify    │  │ - onboarding │  │ - cache        │
│ - sessions  │  │              │  │ - batching     │
└──────┬──────┘  └──────┬───────┘  └────────┬───────┘
       │                │                   │
       ▼                ▼                   │
┌────────────────────────────────────────┐  │
│          SQLite Database                │  │
│            (app.db)                     │  │
│  ┌─────────────────────────────────┐   │  │
│  │ Tables:                         │   │  │
│  │ - users                         │   │  │
│  │ - tickets                       │   │  │
│  │ - access_requests               │   │  │
│  │ - onboarding_checklists         │   │  │
│  └─────────────────────────────────┘   │  │
└────────────────────────────────────────┘  │
                                            │
                                            ▼
                              ┌──────────────────────┐
                              │   OpenAI API         │
                              │   (gpt-4o-mini)      │
                              │                      │
                              │ - Chat completions   │
                              │ - Token optimization │
                              └──────────────────────┘
```

## Use Case Flow Example

### Use Case 1: "I need access to Figma"

```
1. User types message in chat
   │
   ▼
2. Frontend: POST /api/chat/access
   │ Body: { message: "I need access to Figma" }
   │
   ▼
3. Server: requireAuth middleware
   │ ✓ Session valid
   │
   ▼
4. PromptManager: Build AI prompt
   │ Template: "access" scenario
   │ Prompt: "You are an IT Access Coordinator..."
   │
   ▼
5. Cache check
   │ ✗ Not cached
   │
   ▼
6. OpenAI API call
   │ Model: gpt-4o-mini
   │ Response: "Access summary... resource: Figma..."
   │
   ▼
7. Parse AI response
   │ Extract: resourceName = "Figma"
   │
   ▼
8. accessRequestService.createAccessRequest()
   │ INSERT INTO access_requests...
   │ Returns: requestId = 123
   │
   ▼
9. Cache result
   │ Key: hash(prompt)
   │ Value: AI response
   │
   ▼
10. Return JSON to frontend
    │ { aiResponse, action, requestId, resourceName }
    │
    ▼
11. Frontend displays message
    │ "✅ Access request created! ID: 123 for Figma"
    │
    ▼
12. User can check "Access Requests" tab
    Status: pending
```

## Security Flow

```
┌─────────────┐
│   Signup    │
│   Request   │
└──────┬──────┘
       │ POST /api/auth/signup
       │ { email, password, fullName, role }
       ▼
┌──────────────────────┐
│  authService         │
│  - Hash password     │─────► bcrypt.hash(pwd, 10)
│  - Insert to DB      │
│  - Create session    │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Session Cookie      │
│  - Stored in browser │
│  - HttpOnly          │
│  - 24h expiry        │
└──────┬───────────────┘
       │
       │ All subsequent requests
       │ include session cookie
       ▼
┌──────────────────────┐
│  requireAuth()       │
│  - Check session     │
│  - Load user         │
│  - Attach to req     │
└──────────────────────┘
```

## Optimization Strategy Flow

```
User Request
     │
     ▼
┌─────────────────┐
│ Cache Check     │
│ (LRU)           │
└────┬────────────┘
     │
     ├─► Cache Hit ────► Return cached response
     │                   (Save API call & cost)
     │
     └─► Cache Miss
           │
           ▼
     ┌──────────────┐
     │ Context Trim │  (Keep last 8000 chars)
     │ (if needed)  │
     └──────┬───────┘
           │
           ▼
     ┌──────────────┐
     │  OpenAI API  │
     │  Call        │
     └──────┬───────┘
           │
           ▼
     ┌──────────────┐
     │  Store in    │
     │  Cache       │
     └──────┬───────┘
           │
           ▼
     Return response
```

## Role-Based Access Flow

```
GET /api/tickets
     │
     ▼
┌────────────────┐
│ requireAuth()  │
│ Check session  │
└────┬───────────┘
     │
     ▼
┌────────────────────────────┐
│ Load current user from DB  │
│ Get role                   │
└────┬───────────────────────┘
     │
     ├─► Role: employee ──► Return user's own tickets only
     │
     ├─► Role: it_staff ──► Return ALL tickets
     │                       (filter by status/date if requested)
     │
     └─► Role: admin ────► Return ALL tickets with full access
```

This visual documentation helps understand how data flows through the system and how different components interact with each other.
